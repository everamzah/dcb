inventory_plus = {}

function inventory_plus.get_formspec(player, page)
	--return default.gui_survival_form
end

function inventory_plus.set_inventory_formspec(player, formspec)
	--return default.gui_survival_form
end

function inventory_plus.register_button(player,str1, str2)
end
